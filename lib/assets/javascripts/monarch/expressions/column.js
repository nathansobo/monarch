(function(Monarch) {
  Monarch.Expressions.Column = new JS.Class('Monarch.Expressions.Column', {
    initialize: function(name, type) {
      this.name = name;
      this.type = type;
    }
  });
})(Monarch);
